# Frontend runtime config (safe to expose in the browser bundle)
VITE_GENLAYER_CHAIN=bradbury
VITE_LUMEN_CONTRACT_ADDRESS=

# Deployment-only secret. NEVER prefix with VITE_ (that would ship it to the browser).
# Used only by scripts/deploy.mjs, run locally from the terminal — never committed.
DEPLOYER_PRIVATE_KEY=
